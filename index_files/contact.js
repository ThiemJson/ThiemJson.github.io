const toggle=document.querySelector('.nav-toggle'),links=document.querySelector('.nav-links');toggle?.addEventListener('click',()=>{const open=links.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});links?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{links.classList.remove('open');toggle?.setAttribute('aria-expanded','false')}));
const observer='IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12}):null;document.querySelectorAll('.reveal').forEach(element=>observer?observer.observe(element):element.classList.add('visible'));document.querySelector('#year').textContent=new Date().getFullYear();
const form=document.querySelector('#contact-form'),note=document.querySelector('#form-note');form?.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const data=new FormData(form),sender=String(data.get('name')).trim(),email=String(data.get('email')).trim(),topic=String(data.get('subject')).trim(),message=String(data.get('message')).trim(),subject=encodeURIComponent(`[Website] ${topic} — ${sender}`),body=encodeURIComponent(`Hi Thiem,\n\n${message}\n\nBest,\n${sender}\n${email}`);note.textContent='Opening your email app…';window.location.href=`mailto:nguyencaothiem@gmail.com?subject=${subject}&body=${body}`;window.setTimeout(()=>{note.textContent='Your email app opened with this message ready to send.'},700)});
